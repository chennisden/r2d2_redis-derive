This is my fork of redis-derive that uses `r2d2_redis::redis` instead of just `redis`. It's a fairly hacky solution, and hopefully it won't be necessary when the official Redis driver supports pooling.
